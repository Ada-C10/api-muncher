<ul>
  <% @recipes.each do |recipe| %>
  <li> <%= link_to image_tag(recipe.image), recipe.url%> </li>
  <li> <%= link_to recipe.label, recipe.url %> </li>
  <% end %>
</ul>

<%= will_paginate @recipes %>

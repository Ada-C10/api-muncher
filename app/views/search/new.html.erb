<h1>Send Message to <span class="channel-name"> <%= @channel %></span> </h1>

<%= form_tag chat_create_path(@channel), method: "post" do %>
  <%= label_tag(:message, "Search for a recipe") %>
  <%= text_field_tag(:message) %>
  <%= submit_tag("Search") %>

<% end %>

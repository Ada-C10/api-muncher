<h1>Recipes#index</h1>

<section class="recipes-list">
  <h2>Results for "<%= @query %>"</h2>
  <% if @recipes %>
    <ul>
      <% @recipes.each do |recipe| %>
        <li>
          <%= "#{recipe.label}" %>
        </li>
      <% end %>
    </ul>
  <% end %>
</section>

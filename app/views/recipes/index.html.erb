hello
<br />
<% @recipes.each do |recipe| %>
<!-- encode :recipe show address using the name (remove spaces) -- or get an id? -->
<%= link_to recipe.name, recipe_path(recipe.name) %>
<%= recipe.name %>
<!-- image_tag "recipe.photo", alt: "Image of #{recipe.name}" -->
<% end %>

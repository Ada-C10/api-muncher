hello
<br />
<% @recipes.each do |recipe| %>
<!-- encode :recipe show address using the name (remove spaces) -- or get an id? -->
<%= link_to recipe.name, recipe_path(recipe.uri_num) %>
<%= image_tag(recipe.photo, alt: "Food image: #{recipe.name}") %>
<%= recipe.uri_num %>


<% end %>

<%= will_paginate @recipes %>

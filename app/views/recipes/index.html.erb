<section class="recipes-list">
  <h1>Your List of <%= "#{@food_search}" %> Recipes </h1>
  <% if @recipes %>
    <ul>
      <% @recipes.each do |recipe| %>
        <li>
          <%= recipe.name %>
        </li>
      <% end %>
    </ul>
  <% end %>
</section>

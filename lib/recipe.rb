class Recipe

  def initialize(label, image, url, dietLabels, healthLabels, ingredientLines)
    @label = label
    @image = image
    @url = url
    @dietLabels = dietLabels
    @healthLabels = healthLabels
    @ingredientLines = ingredientLines

  end

end

require 'test_helper'

describe "Recipe" do
  it "Cannot be initialized with less than 6 parameters" do
    
  end

  it "Must initialize name, image, uri, url, ingredients, and dietLabels properly" do

  end
end
